---
version: alpha
name: oh-my-llm
description: 面向长文本阅读与创作的本地 Flutter 工作台
colors:
  primary-light: '#4F46E5'
  primary-dark: '#818CF8'
  background-light: '#F5F6F8'
  background-dark: '#121418'
  card-light: '#FFFFFF'
  card-dark: '#1B1D22'
typography:
  sans:
    fontFamily: 'Noto Sans SC'
spacing:
  small: '8px'
  medium: '16px'
  large: '24px'
rounded:
  control: '8px'
  card: '12px'
  dialog: '16px'
components:
  button: {}
  card: {}
  input: {}
  dialog: {}
---

# 设计约定

## 产品与视觉方向

Windows、Android 双端中文本地 LLM 客户端，主要任务是长文本阅读、对话与写作。产品界面使用简体中文，模型与协议名称保留原文。不依据语言字符推断其他市场的业务规则。

Material 提供交互、焦点与可访问性基础。视觉以中性阅读画布、紧凑工具栏和清晰的内容分组为主；紫蓝强调色用于选中、主要动作和焦点。通过布局区分上下文选择、发送配置与编辑表单，不把每项功能都包成一张卡片，不靠缩小正文提高密度。

本文件维护视觉与交互约定；详细业务语义由 [AGENTS.md](AGENTS.md) 与领域规格维护，不在此复制实现细节。运行时 token 为唯一数值来源（Model B），不从本文生成 Dart 代码。

## 主题与 token

| 范畴 | 唯一来源 | 使用规则 |
| --- | --- | --- |
| 颜色、字体、控件外观 | `lib/app/theme/app_theme.dart` | 浅／深色分别使用 seed；中性 surface 与背景显式定义，语义前景和错误色从 ColorScheme 派生。页面消费 Theme，不复制颜色。 |
| 间距、圆角、限宽、命中区 | `lib/core/constants/app_layout_tokens.dart` | 常规边距 16、紧凑间距 8；控件圆角 8、卡片 12、弹窗 16。旧 lg/xl token 不作为新控件的默认形状。 |
| 响应式职责 | `lib/core/constants/app_breakpoints.dart` | shell、content、form、bubble 各用对应断点。除 shell 外，根据 LayoutBuilder 的父级约束决策，不按设备或平台名称切布局。 |

思源黑体为默认字体。正文、区域标题和状态分别使用 `bodyMedium`、`titleSmall/titleMedium`、`bodySmall`；用户字号偏好继续生效。长文保留阅读宽度与行距，内容可选择复制。错误同时带文字和图标，不仅靠颜色。次级操作不与发送／保存争夺强调。

卡片只包裹有意义的内容实体；消息列表、侧栏与设置分组采用平铺、标题或分隔线。弹窗和菜单使用 Material overlay 与统一 surface，不叠加背景模糊或装饰动画。

## 布局与组件所有权

| 能力 | 规范实现 | 行为与变体 |
| --- | --- | --- |
| 导航 | `AppShellScaffold` / `AppDestination` / GoRouter | 页面级操作进入 AppBar；窄屏用 `AppAdaptiveActions` 收纳低频动作。对象动作紧邻所属对象。 |
| 上下文选择 | Flutter `DropdownButton` | 作品、会话有 label 和有限宽度；相邻新建动作成组，空间不足按组换行。长值省略并可通过 tooltip／打开列表查阅。 |
| 发送配置 | `ComposerTemplateHeader` / `ComposerProviderModelRow` | 桌面模板、服务商、模型同排，模型获得更宽比例；窄屏换行。选择器内部可 isExpanded，父级不能无条件占满画布。 |
| 短表单字段 | `AppFieldGroup` + `DropdownButtonFormField` / `TextField` | 默认字段上限 320，按可用宽度换行；短枚举不占整个弹窗。URL、密钥、长正文按编辑需要保留宽度。 |
| 弹窗 | `SettingsFormDialogScaffold` / Material `AlertDialog` | 固定标题、动作，内容内部滚动。Agent 文档只有正文编辑区滚动，键盘弹出后保存仍可达。 |
| 确认 | `AppConfirmDialog` | 说明对象与后果，取消默认聚焦。删除服务商说明受影响模型数量，删除模型说明所属服务商；聊天记录保留。 |
| 设置分组与列表 | `SettingsSectionCard` / `ProviderTile` | 分组使用标题、说明和留白；服务商、模型使用平坦记录。低频编辑／删除进菜单，展开后显示详情。 |
| 网格与主从 | `AppAdaptiveGrid` / `AdaptiveMasterDetailLayout` | 使用内容规格与语义断点，不按固定设备列数模拟适配。 |
| 滚动与状态 | Flutter ScrollBehavior / ScrollbarTheme、feature 状态 | 保留可操作滚动条，各内容区拥有滚动；真实终态驱动完成、失败、中断及停止反馈。 |

选择菜单由 Flutter 绘制，使用应用主题与圆角，不另造选择器框架。职责只有少量稳定选项时，Agent 配置桌面采用左侧职责导航、右侧模型与提示词；窄屏保留下拉选择职责。共享规则独立分组。确认／重命名用小弹窗，复杂编辑按内容限宽，不给所有任务统一一个窄宽度。

按钮使用 Filled 表示主要动作，Text／Outlined 表示次级动作，IconButton 必须有 tooltip。沿用 Material 的键盘、hover、focus、disabled 语义；自绘选中视图补充 selected 语义，不重复包无意义 Semantics。操作不能只能靠 hover 发现，停止入口持续可达。表单保留真实 label、错误文字及失败输入，运行中不能重复提交。

## 对话工作区

历史侧栏只保留一层区域标题。消息作者和动作位于**头部**；复制、收藏、编辑／重试直接可达，排除与删除进入消息操作菜单。推理、正文、inline 错误各自呈现，不把操作移到消息尾部，不把错误藏进临时通知。

输入区按“发送配置 → 模板变量（需要时）→ 正文 → 发送与次级设置”组织。模板变量按类型约束宽度，数字字段保留紧凑形式。桌面缓存命中率进入工具栏提示，窄屏进入更多设置；更多设置按钮标签保持简短稳定。保留草稿、编辑事务和模板变量隔离，不扩大流式状态的监听范围。Enter 换行、Ctrl+Enter 发送；IME 组合输入期间不得触发发送。

API Key 编辑字段**保持明文**，便于用户复制；这是明确的产品选择，不增加遮罩或显隐开关。输入建议保持关闭，列表摘要与网络日志沿用既有脱敏策略，不把凭据放进通知。

## Agent 工作区

顶部按作品／会话选择、正文／工作文档／剧情状态视图、运行状态组织。模型与规则、上下文预览在 AppBar；总结管理归会话操作。运行中禁用上下文切换与配置修改，仍可查看运行子任务、停止整棵任务树。输入提示根据实际交互方式显示键盘快捷键。

正文优先于执行诊断：用户任务为轻背景块，Agent 正文直接呈现。writer 完成审查与状态提交后，主任务和 writer 显示同一份采用正文，整轮过程默认折叠但可展开；推理与工具详情各自展开。工具开始时即显示，不能等子任务完成才出现。

主会话与子任务共用 `AgentTranscript` 手动滚动，不自动跟随输出；“回到最新”只定位一次。主视图显示最近 50 个根任务及完整后代，总结根任务只在总结管理显示，数据库保留全部记录。子任务可直接回读，返回直接父任务，切换详情不停止运行；用量包含全部后代。

同一作品共享工作文档和剧情状态，会话独立拥有草稿、历史和配置快照。新设定在下次运行采用，每轮重建资料前缀。保存方案和应用配置分别表示持久化与会话采用。运行中的配置只读；配置和文档关闭前确认未保存编辑。人物卡保存完整作者设定，动态状态由状态 Agent 根据正式正文维护。

剧情状态与正文的事务、失败后重试／放弃、撤回最新一轮，遵循 [剧情状态规格](docs/specs/2026-09-15-agent-story-state.md)。状态更新失败保留正文；撤回恢复原指令，只有覆盖新草稿才确认。逻辑表只读、长文本纵向展示，每表先显示 20 行并可加载更多；历史入口可回看撤回记录，不增加逐条状态审批。

总结管理遵循 [委派上下文规格](docs/specs/2026-09-16-agent-delegation-context.md)。用户主动选连续楼数，可隐藏、总结替代、编辑、重新总结、恢复原文；不自动总结。长列表与来源正文在弹窗内滚动，关闭／停止固定可达，关闭不会取消任务。运行中禁止切换来源，编辑未保存退出需确认；原文、状态与原生工具／推理副本保留。

发送前使用“下一次输入预览”，模型步骤显示实际保存的输入快照。旧记录缺少边界时明确不可恢复；字符／字节数不称 Token。顶部合计最近任务中已报告用量，缺失则说明不完整，详情显示实际模型。角色资料范围由委派绑定，已有动态人物可由状态行参与推演，无需额外知情副本。

## 验证与维护

修改共享外观时同时检查 Chat、Agent 和设置等相邻页面；覆盖浅／深色、桌面／窄屏、空与有内容、长名称、打开的菜单与弹窗、键盘／焦点、输入法、键盘弹出和大字号。临时截图与诊断放在 ignored 的 `logs/`，几何试验不固化为像素级业务测试。

永久测试保护草稿隔离、模板变量、发送／停止、消息树、删除确认和弹窗保存／放弃等行为；格式化、静态分析、导入边界、全量测试与构建作为回归证据。离线渲染不能替代 Windows 原生与 Android 真机验收，交付中应说明实际验证边界。
